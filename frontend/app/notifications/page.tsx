"use client";
import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { Bell, CheckCheck, RefreshCw } from "lucide-react";
import { apiGet, apiPatch, normalizeList } from "@/lib/api";
type Notification={id:number;title:string;message:string;kind:string;read:boolean;link:string;created_at:string};
export default function NotificationsPage(){const[rows,setRows]=useState<Notification[]>([]);const[message,setMessage]=useState("");const load=useCallback(async()=>{try{setRows(normalizeList(await apiGet<Notification[]>("/collaboration/notifications/?page_size=250")));setMessage("")}catch(e){setMessage(e instanceof Error?e.message:"Notifications could not be loaded")}},[]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer)},[load]);async function mark(id:number,read=true){await apiPatch(`/collaboration/notifications/${id}/`,{read});await load()}async function markAll(){await Promise.all(rows.filter(r=>!r.read).map(r=>apiPatch(`/collaboration/notifications/${r.id}/`,{read:true})));await load()}return <div className="page-stack"><header className="module-header"><div><span className="eyebrow">NOTIFICATION CENTER</span><h1>Invitations, access changes, and collaboration updates</h1><p>Every important action links directly to the relevant workspace.</p></div><div className="row-actions"><button className="secondary-button" onClick={()=>void load()}><RefreshCw size={16}/> Refresh</button><button className="primary-button" onClick={()=>void markAll()}><CheckCheck size={16}/> Mark all read</button></div></header>{message&&<div className="system-banner warning">{message}</div>}<section className="data-panel notification-center-list">{!rows.length?<div className="table-state"><Bell/><strong>No notifications yet</strong><p>Invitations, assignments, and Project Room activity will appear here.</p></div>:rows.map(row=><article className={row.read?"notification-read":""} key={row.id}><Bell size={18}/><div><span>{row.kind.replaceAll("_"," ")}</span><h3>{row.title}</h3><p>{row.message}</p><small>{new Date(row.created_at).toLocaleString()}</small></div><div className="row-actions">{row.link&&<Link className="primary-button" href={row.link}>Open</Link>}<button className="secondary-button" onClick={()=>void mark(row.id,!row.read)}>{row.read?"Mark unread":"Mark read"}</button></div></article>)}</section></div>}
