# ForgeGov v2.0.1.1 TypeScript Hotfix

This hotfix restores the frontend type definitions required by the Teaming workspace:

- `PartnerRow`
- `TeamingRequestRow`
- `TeamingActivityRow`

The definitions match the fields consumed by the React components and the Django REST serializers. No backend behavior or database schema was changed.
