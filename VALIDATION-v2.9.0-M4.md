# v2.9.0-M4 validation

Source-level validation completed. Local Docker remains the release gate: migrations, Django checks/tests, lint, TypeScript, Next build, then browser regression of Command Center and Executive Capture.
